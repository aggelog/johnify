import React from 'react';

import './styles.css';

const NotFoundGif = () => (
  <div className="application-not-found__container">
    <h3>Sorry, couldn't found gif</h3>
    <h4>We are the best gif engine, but it needs to exist</h4>
  </div>
);

export default NotFoundGif;
